{
  "schema_version": "ao.foundry.task.v0.1",
  "task_id": "clean-clone-smoke",
  "title": "Clean clone smoke verification",
  "objective": "Verify AO Foundry public fixtures and release dry-run without sibling repositories or remote services.",
  "priority": "normal",
  "state": "queued",
  "target_repos": ["ao-foundry"],
  "required_delegations": [
    {
      "delegate_to": "ao-forge",
      "reason": "Governed implementation remains delegated to AO Forge even when the smoke path only verifies public fixtures."
    }
  ],
  "acceptance": [
    "Public registry and task fixtures validate",
    "Readiness audit writes public evidence",
    "Release dry-run writes a checksum manifest"
  ],
  "verification": ["go test ./..."],
  "safety": {
    "local_only": true,
    "allow_network": false,
    "allow_release_mutation": false,
    "allowed_write_roots": ["../ao-foundry"],
    "forbidden_actions": ["push", "tag", "publish-release", "upload-artifacts", "credential-access"]
  }
}
