# Reproducibility Runbook

1. Read `causal-influence-trace.json`.
2. Confirm the prior signal names a measured blocker or result.
3. Confirm the candidate follows from that prior signal.
4. Confirm `measurement.json` records baseline and post-change scores.
5. Confirm `eval-regression.json` and `rollback-proof.json` passed.
6. Confirm no artifact claims broad_RSI or unrestricted authority.

Expected result: causal chain reproducibility runbook quality is reproducible as a bounded public-safe causal-chain evidence cycle.
