# Attempt G: external-command allowlist readback quality

1. Inspect `measurement.json`.
2. Verify `no_actual_external_execution`, `no_provider_calls`, and `no_credential_use` are true.
3. Confirm rollback evidence under `../../rollback/attempt-g-external-command-allowlist-readback-quality.json`.
4. Confirm negative-control evidence under `../../negative-controls/attempt-g-external-command-allowlist-readback-quality.json`.
5. Confirm final rollup preserves all denied surfaces.
